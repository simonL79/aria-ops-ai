name: Dependency Audit

# Proactive supply-chain alerting: runs weekly and on every dependency change.
# Fails (and therefore surfaces an alert/notification) when a high or critical
# severity advisory is found, so upstream fixes can be applied promptly.
on:
  schedule:
    - cron: "0 7 * * 1" # Mondays 07:00 UTC
  push:
    paths:
      - "package.json"
      - "package-lock.json"
      - "bun.lockb"
  pull_request:
    paths:
      - "package.json"
      - "package-lock.json"
      - "bun.lockb"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies (no scripts)
        run: npm ci --ignore-scripts

      # Capture the audit result without aborting the job, so we can post a
      # Slack alert before failing the run.
      - name: Audit (high + critical only)
        id: audit
        run: |
          set +e
          REPORT="$(npm audit --audit-level=high 2>&1)"
          STATUS=$?
          echo "$REPORT"
          # Persist a trimmed report for the Slack step.
          {
            echo "report<<EOF"
            echo "$REPORT" | tail -n 40
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Notify Slack on high/critical findings
        if: steps.audit.outputs.status != '0' && env.SLACK_WEBHOOK_URL != ''
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          REPORT: ${{ steps.audit.outputs.report }}
        run: |
          PAYLOAD=$(jq -n \
            --arg repo "$GITHUB_REPOSITORY" \
            --arg run "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID" \
            --arg report "$REPORT" \
            '{
              text: ":rotating_light: *Dependency audit found high/critical vulnerabilities*",
              blocks: [
                { type: "section", text: { type: "mrkdwn", text: (":rotating_light: *High/critical dependency vulnerabilities in* `" + $repo + "`") } },
                { type: "section", text: { type: "mrkdwn", text: ("```" + $report + "```") } },
                { type: "section", text: { type: "mrkdwn", text: ("<" + $run + "|View workflow run>") } }
              ]
            }')
          curl -sf -X POST -H 'Content-type: application/json' --data "$PAYLOAD" "$SLACK_WEBHOOK_URL"

      # Open (or update) a GitHub Issue so the failure is tracked and actionable,
      # with a direct link to the workflow run that detected it.
      - name: Open GitHub Issue on high/critical findings
        if: steps.audit.outputs.status != '0'
        uses: actions/github-script@v7
        env:
          REPORT: ${{ steps.audit.outputs.report }}
        with:
          script: |
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${process.env.GITHUB_REPOSITORY}/actions/runs/${process.env.GITHUB_RUN_ID}`;
            const report = process.env.REPORT || 'See workflow logs for details.';
            const label = 'dependency-audit';
            const title = '🚨 Dependency audit: high/critical vulnerabilities detected';
            const body = [
              `A scheduled/triggered dependency audit failed with **high or critical** severity findings.`,
              ``,
              `**Workflow run:** ${runUrl}`,
              ``,
              `<details><summary>Audit report (trimmed)</summary>`,
              ``,
              '```',
              report,
              '```',
              ``,
              `</details>`,
              ``,
              `_Opened automatically by the Dependency Audit workflow._`,
            ].join('\n');

            // Ensure the label exists (ignore error if it already does).
            try {
              await github.rest.issues.createLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: label,
                color: 'd73a4a',
                description: 'Automated dependency audit findings',
              });
            } catch (e) { /* label likely already exists */ }

            // Reuse an existing open issue if present, otherwise create a new one.
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: label,
              per_page: 1,
            });

            if (existing.data.length > 0) {
              const number = existing.data[0].number;
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: number,
                body,
              });
              core.info(`Commented on existing issue #${number}`);
            } else {
              const created = await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: [label],
              });
              core.info(`Created issue #${created.data.number}`);
            }

      - name: Fail if vulnerabilities were found
        if: steps.audit.outputs.status != '0'
        run: |
          echo "High or critical vulnerabilities detected. See the audit report above."
          exit 1

