
import ThreatClassifierContainer from "./threatClassifier/ThreatClassifierContainer";

// Re-export the component with the same interface for backward compatibility
export default ThreatClassifierContainer;
